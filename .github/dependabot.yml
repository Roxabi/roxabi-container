version: 2
updates:
  - package-ecosystem: docker
    directory: /images/ml-base
    schedule:
      interval: weekly
    labels:
      - dependencies
    cooldown:
      default-days: 3
      semver-major-days: 3
      semver-minor-days: 3
      semver-patch-days: 3

  - package-ecosystem: docker
    directory: /images/base
    schedule:
      interval: weekly
    labels:
      - dependencies
    cooldown:
      default-days: 3
      semver-major-days: 3
      semver-minor-days: 3
      semver-patch-days: 3
